const path = require("path");

module.exports = {
    config: path.resolve("src", "config", "migration.config.cjs"),
    "migrations-path": path.resolve("migrations"),
    "seeders-path": path.resolve("seeders"),
};
